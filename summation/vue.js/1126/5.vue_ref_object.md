# vue_ref_object

## 🔵 Vue 기본 구조

### ✔ createApp()에 전달되는 객체 = Vue 컴포넌트

### ✔ 컴포넌트 상태는 반드시 setup() 내부에서 정의하고 **객체를 return** 해야 함

```js
const app = createApp({
  setup() {
    const message = ref('Hello vue!')

    return {
      message
    }
  }
})
```

---

## 🟦 템플릿 렌더링 (Template Rendering)

### ✔ 반응형(ref) 변수는 템플릿에서 그대로 사용 가능

### ✔ Mustache Syntax (`{{ }}`)로 동적 텍스트 표현

```html
<div id="app">
  <h1>{{ message }}</h1>
</div>
```

### ✔ 템플릿에서는 유효한 JavaScript 표현식도 사용 가능

```html
<h1>{{ message.split('').reverse().join('') }}</h1>
```

> 🔹 message 변화 → 화면이 자동으로 업데이트 (반응형 시스템)

---

# 🔵 Event Listeners in Vue

### ✔ `v-on` 디렉티브로 DOM 이벤트 연결 가능

### ✔ 이벤트 내부에서 ref 변수를 수정 → 화면 상태 자동 업데이트

#### HTML

```html
<div id="app">
  <button v-on:click="increment">버튼</button>
  <p>{{ number }}</p>
  <p>{{ number }}</p>
  <p>{{ number }}</p>
</div>
```

#### JS

```js
const { createApp, ref } = Vue

const app = createApp({
  setup() {
    const number = ref(0)

    const increment = function () {
      number.value++
    }

    return {
      number,
      increment
    }
  }
})
```

---

# 🟩 ref 객체가 필요한 이유

### ❓ “왜 일반 변수 대신 ref 객체를 써야 하나?”

### ✔ 1. 일반 JS 변수는 Vue가 감지할 수 없음

→ 때문에 값이 바뀌어도 DOM 업데이트가 되지 않음

### ✔ 2. Vue는 템플릿에서 사용된 모든 ref 값을 추적함

→ ref가 변경되면 해당 ref를 사용하는 컴포넌트만 자동으로 다시 렌더링됨

### ✔ 3. 이를 위해 **참조형 객체(ref)** 로 감싸는 구조가 필요

JS 자체는 변수를 자동 감지하는 기능이 없기 때문

🔗 참고:
> [반응형 기본 관련 Vue Doc 문서](https://ko.vuejs.org/guide/essentials/reactivity-fundamentals)

---

# 🟦 반응형 변수 vs 일반 변수

### ✔ ref 값이 바뀌면 화면은 자동 업데이트

### ✔ 일반 변수는 값이 바뀌어도 화면이 갱신되지 않음

#### JS

```js
const app = createApp({
  setup() {
    const reactiveValue = ref(0)
    let normalValue = 0

    const updateValues = function () {
      reactiveValue.value++
      normalValue++
    }

    return {
      reactiveValue,
      normalValue,
      updateValues
    }
  }
})
```

#### HTML

```html
<div id="app">
  <p>반응형 변수: {{ reactiveValue }}</p>
  <p>일반 변수: {{ normalValue }}</p>

  <button v-on:click="updateValues">
    값 업데이트
  </button>
</div>
```

> ➜ reactiveValue만 증가하면 화면이 즉시 갱신됨
> ➜ normalValue는 값은 증가해도 화면은 그대로

---

# ✨ 요약 정리

| 개념                    | 설명                             |
| --------------------- | ------------------------------ |
| **createApp()**       | Vue 애플리케이션 인스턴스 생성             |
| **setup()**           | 컴포넌트의 시작점, 데이터·함수 정의           |
| **ref()**             | 반응형 상태를 만드는 함수 (값은 `.value`)   |
| **Mustache(`{{ }}`)** | 템플릿에서 JS 표현식 사용                |
| **v-on**              | 이벤트 리스너 (예: v-on:click)        |
| **Ref vs 일반 변수**      | ref는 변경 → 자동 렌더링, 일반 변수는 렌더링 X |
